import { HeaderMenu } from "./components/HeaderMenu.tsx";
import React from "react";

export const Header = () => {
  return (
    <header className="header">
      <HeaderMenu />
    </header>
  );
};
